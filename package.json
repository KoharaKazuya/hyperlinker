{
  "name": "hyperlinker",
  "description": "creates hyperlinks from plain text on website",
  "version": "2.0.0",
  "devDependencies": {
    "rollup": "^2.44.0",
    "rollup-plugin-terser": "^7.0.2"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rollup -c && cp -r src/{manifest.json,assets} dist/"
  }
}
